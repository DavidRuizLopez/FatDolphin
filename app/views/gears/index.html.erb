<% if @gears != []  %>
  <h1>See our list of <%= params[:query] %></h1>

  <div class="cards">
      <div class="row row-cols-1 row-cols-md-3 justify-content-center" >
        <% @gears.each do |gear| %>
              <%= link_to gear_path(gear) do %>
              <div class="card mb-3">
                <img class="card-img-top" src="https://images.unsplash.com/photo-1518353053542-7ea33d942319?ixlib=rb-1.2.1&auto=format&fit=crop&w=1351&q=80" alt="Card image cap">
                <div class="card-body">
                  <h5 class="card-title"> <%= gear.name %> </h5>
                  <p class="price text-right"> $<%= gear.price %> </p>
                </div>
              </div>
              <% end %>
        <% end %>
      </div>

    </div>
  </div>

<% else %>
  <p>Sorry! but <%= params[:query] %> is not one of our categories!</p>
<% end %>

<%= link_to "Go back", root_path, class: "btn"%>

